# Documentation

Native-app documentation lands here as the phases deliver it:

- `docs/spikes/` — WebKitGTK × Lightweight Charts decision spike (P0-T05)
- `docs/ui/` — component reference, perf notes (P1+)
- `docs/engine/` — order types, tick resolution semantics (P3/P6)
- `docs/perf/` — benchmark records vs the §8 budget (P4+)
- `docs/datasources/` — provider research notes (P2)

Until then, the source of truth is `NATIVE_PLAN.md` (product spec) and
`EXECUTION_PLAN.md` (execution protocol + task graph).
